FROM python:3.13-slim
RUN pip install --no-cache-dir "fastapi>=0.115" "uvicorn>=0.30" && useradd -u 10001 -m agent
USER 10001
COPY main.py /app/main.py
WORKDIR /app
EXPOSE 8080
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8080"]
